export class User {
	constructor(userId, userName, userPassword, userMail, userSms){
		this.userId = userId;
		this.userName = userName;
		this.userPassword = userPassword;
		this.userMail = userMail;
		this.userSms = userSms;
	}
}
